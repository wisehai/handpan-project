node_modules/
*.ipa
# the native Xcode project is never committed — it depends on macOS/CocoaPods
# tooling that can't run on this machine, so it's regenerated fresh on every
# Codemagic build via `npx cap add ios` (see README.md)
ios/
# generated copy of ../handpan-player.html and ../vendor/pdfjs/ (see README's
# rebuild steps) — not source
www/index.html
www/vendor/
# local journal directory some codemagic-cli-tools / xcodebuild invocation
# creates under ios-app/ — not something we created intentionally
.vmark/
